import React, { useState, useEffect } from "react";


// pass in userGoogleID
// pass in representationID
// we also need to know whether it's purchased or not
// on click we run a callback function
// calling it will either open up a popup
// for each one check whether it's the active
const RepresentationCatalogItem = (props) => {

	return (
		<div>

		</div>
	);
};

export default RepresentationCatalogItem;